import { CartItemsFilterPipe } from './cart-items-filter.pipe';

describe('CartItemsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CartItemsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
