import { CartItemsTotalPipe } from './cart-items-total.pipe';

describe('CartItemsTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new CartItemsTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
